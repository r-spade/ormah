# Agent brief: benchmark harness implementation

This is the exact prompt handed to the Codex implementation agent
(gpt-6-astra, reasoning effort high) on the ai-agents node on 2026-09-18.
It is kept here so the handoff can be reviewed alongside the result.
The approved design it refers to is [DESIGN.md](DESIGN.md).

Differences between the brief and what the agent delivered are listed in
the pull request description.

---

You are the implementation agent for the public memory benchmark harness in r-spade/ormah.

Work only in the current git worktree (branch feat/memory-benchmarks, already checked out and pushed). There is no AGENTS.md or CLAUDE.md in this repo; follow the conventions you observe in the code and tests. Read eval/bench/DESIGN.md completely: it is the approved design (including the LLM providers section) and this brief only adds operational detail. Your first attempt stopped because the design was not committed; it is now. Study eval/recall/ (cli.py, runner.py, seeder.py, metrics.py, report.py), eval/settings.py, tests/test_eval_recall/, the eval wiring in src/ormah/cli.py, MemoryEngine.recall_search_structured and ingest_conversation in src/ormah/engine/memory_engine.py, src/ormah/embeddings/vector_store.py (upsert_batch) and src/ormah/embeddings/encoder.py before writing code. Plan first, then implement.

Hard rules:
- Never call the paid Anthropic API. There is no ANTHROPIC_API_KEY on this machine and you must not add one. The `anthropic` provider is unit-tested with fakes only. The subscription-billed `claude-cli` and `codex` providers are real and you may exercise them, but only for the smoke runs listed under deliverable 8. `claude -p` is logged in here; use `--bare --no-session-persistence --output-format json` and turn off tool use for these plain question-answering calls (see `claude --help` for the tools flags). The JSON result carries `result`, `usage` and `total_cost_usd` (an estimate; record it but label it estimate). For `codex` use `/root/agent-tools/codex-0.154.0/node_modules/.bin/codex exec` with `--ignore-user-config`, `--sandbox read-only`, `--color never`, prompt on stdin and `--output-last-message <file>`; parse the file.
- Do not merge, release, modify main, or write Ormah memories from this node.
- Do not commit anything under eval/bench/data/, eval/bench/artifacts/ or eval/bench/eval_db/ (gitignored). Do not commit dataset content into tests; tests use tiny synthetic fixtures.
- Keep memory use under 3 GB: stream or index the 278 MB LongMemEval JSON sensibly (loading it once is fine; do not duplicate it), and free per-question haystacks after use.
- Follow existing conventions: lazy imports in src/ormah/cli.py with the same "not installed in the published runtime" guard, isolated engine via eval.settings.RETRIEVAL_EVAL_SETTINGS_OVERRIDES, Ruff clean, conventional commits.
- The broad test suite fails at collection here because src/ormah/main.py opens a log file under /root/.local/share/ormah/logs; that directory is now writable for you. If it still fails, redirect ormah.server_manager.LOG_DIR to a temp dir for the run and say so in the handoff.

Deliverables:
1. eval/bench/ package implementing the design: datasets.py (LongMemEval-S and LoCoMo adapters to one common Question/Session shape, download subcommand with SHA-256 manifest), store.py (raw and extract modes, extraction cache keyed by session id + prompt hash, embedding cache keyed by sha256 of the embedding text, seeding via file store + index + VectorStore.upsert_batch like eval/recall/seeder.py), retrieve.py, providers.py (one `TextProvider` interface with `anthropic`, `claude-cli` and `codex` implementations; tests inject fakes and only check subprocess argument construction), answer.py and judge.py using that interface, plus bench-only `LLMAdapter` subclasses wrapping the same CLI providers for extract mode installed through a small `set_adapter()` helper on ormah.background.llm_client, metrics.py, report.py, manifest/cost accounting, cli.py. Prompts: adapt the answer prompts from https://github.com/mem0ai/memory-benchmarks (benchmarks/locomo/prompts.py and benchmarks/longmemeval/prompts.py) and the official LongMemEval judge prompts from https://github.com/xiaowu0162/LongMemEval/blob/main/src/evaluation/evaluate_qa.py (per question type plus the abstention prompt); cite the sources in comments. Network access is available for fetching those references.
2. CLI wiring exactly as in the design doc: `ormah eval bench download`, `ormah eval bench run longmemeval|locomo ...`, `ormah eval bench report <run_id>`. Free phases (store, retrieve, report) are the default. LLM phases run only when named explicitly; the `anthropic` provider additionally requires ANTHROPIC_API_KEY, otherwise print the estimated cost from the price table and exit non-zero. Enforce --max-usd during anthropic phases using real usage counts.
3. Every phase resumable: rerunning with --run-id and --resume skips completed questions using the on-disk per-question JSONL.
4. Retrieval details: per question clear the eval DB, seed its haystack (LoCoMo: seed once per conversation and reuse for all of its questions), call engine.recall_search_structured(question, limit=k, min_relevance=0.0, auto_temporal=False, default_space=None) and record ranked ids, scores, sources, latency, and the count passing settings.recall_min_relevance_score. Set node created to the session date. Make sure backdated nodes are not dropped by FSRS or tier logic (copy the updated/last_accessed = now trick from eval/recall/seeder.py) and verify with a test.
5. Metrics per the design doc, including session-level recall@k / ndcg@k for LongMemEval (gold = answer_session_ids; abstention questions reported separately, excluded from retrieval recall) and turn-level recall@k for LoCoMo (gold = evidence dia_ids; category 5 excluded from the J-score and reported as abstention rate; category 3 gold cut at first semicolon).
6. eval/bench/README.md: methodology, how to run each track, differences from Mem0 and official setups, limitations (no reranker, auto_temporal off, raw mode for LongMemEval in the first run, subscription CLI models instead of API models).
7. Tests under tests/test_eval_bench/ covering dataset adapters (synthetic fixtures), raw and extract store with a fake LLM, embedding cache hits, retrieval metrics, LoCoMo category rules, LongMemEval abstention handling, cost accounting and --max-usd abort, resume behaviour, provider argument construction, and CLI argument parsing. `uv run pytest tests/test_eval_bench -q` and `uv run ruff check eval/bench src/ormah/cli.py tests/test_eval_bench` must pass, plus the existing eval tests.
8. Run the free track and the subscription smoke runs for real and record the numbers in your handoff:
   - `uv run ormah eval bench run locomo --mode raw --k 30` on the full set (all 10 conversations). Report retrieval recall@30 overall and per category, p50/p95 latency, wall time.
   - LongMemEval: eval/bench/data/longmemeval_s.json is complete (278025796 bytes). Run `uv run ormah eval bench run longmemeval --mode raw --k 30 --limit 50` and report session-level recall@30 and ndcg@30 overall and per question type, plus throughput, and an extrapolated wall time for all 500 questions.
   - Smoke of the LLM phases, subscription-billed, small: `uv run ormah eval bench run locomo --mode extract --extract-provider claude-cli --answer-provider claude-cli --judge-provider codex --phase all --limit 10 --conversation 0` (extract only conversation 0; if `--conversation` is awkward, pick the equivalent smallest scope) and `uv run ormah eval bench run longmemeval --mode raw --answer-provider claude-cli --judge-provider codex --phase all --limit 10`. Report accuracy on those 10, per-call latency, and any provider errors. Stop at 10; the owner will launch the full runs after review.
   Do not commit the artifacts; commit only a results summary under eval/bench/results/ clearly marked as a smoke run.

Verification: focused tests, Ruff, then the broad practical suite (`uv run pytest -q`, excluding integration markers if they need services). Review your own diff for correctness and for anything that could leak private data. Commit conventionally in small commits, push feat/memory-benchmarks, and open a DRAFT pull request against main titled "feat(bench): public LongMemEval/LoCoMo benchmark harness" whose body summarises design, what ran, numbers, and open questions. Do not merge. Write the final handoff to /root/jobs/memory-benchmarks/final.md: what was built, exact commands, measured numbers, per-call latency and estimated token volume for the full LLM runs, and anything the owner must decide before launching them.
